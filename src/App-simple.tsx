// Simple test version
const AppSimple = () => {
  return (
    <div style={{ padding: "40px", fontFamily: "system-ui", background: "#fff", minHeight: "100vh" }}>
      <h1 style={{ fontSize: "24px", marginBottom: "20px" }}>✅ React is Working!</h1>
      <p>If you see this, React is mounting correctly.</p>
      <p style={{ color: "#666", marginTop: "20px" }}>Now let's test the full app...</p>
    </div>
  );
};

export default AppSimple;
